# Green's Functions

:::{admonition} Course-material provenance
:class: dropdown
This page was migrated from the CHM 696 course materials developed by Jonathan Hood and Lyudmila Slipchenko. Equations and examples remain under editorial review during the Fall 2026 website migration.
:::

## Delta Function


- The Dirac delta function $\delta(x)$ is a generalized function or distribution.

- The Dirac delta function is commonly used in physics and engineering to represent idealized point sources or impulses.

- When integrating over it and another function $f(t)$, it \"picks out\" the value of the function at a specific point: $$\int_a^b f(t) \, \delta(t - t_0) \, dt = f(t_0) \quad \text{for } a < t_0 < b$$ This property indicates that the delta function evaluates a function $f(t)$ at $t = t_0$, and outside this interval, the integral is zero.

- The delta function is normalized as follows: $$\int_{-\infty}^{\infty} \delta(x) \, dx = 1$$


## Delta Function (continued)


- While the delta function is not a proper function, it can be viewed as the limit of a family of normalized functions.

- One common representation is in terms of a top-hat functions: $$\Pi_a(x) =
  \begin{cases}
  \frac{1}{a} & \text{if } |x| < \frac{a}{2} \\
  0 & \text{otherwise}
  \end{cases}$$

- Another common representation is in terms of a Gaussian. In the limit as $a \to 0$: $$\delta_a(x) = \frac{1}{|a|\sqrt{\pi}} e^{-x^2/a^2}$$ This approximation is useful in numerical analysis, where $\delta(x)$ can be represented and manipulated as a limit of smooth functions.

- Both of these functions satisfy the normalization condition: $$\int_{-\infty}^{\infty} \delta_a(x) \, dx = 1$$

- As $a \to 0$, these functions converge to the delta function: $\delta(x) = \lim_{a \to 0} \delta_a(x)$


## Properties of the delta function


## Green's functions: Introduction


- The Green's function provides a powerful method for solving inhomogeneous linear equation systems.

- The Green's function generalizes the concept of an inverse matrix or an impulse response to continuous systems.

- The Green's function is fundamentally defined as **the response of the linear system to a unit impulse**.

- Let's write the linear equation system as $Ly(x)=f(x)$, where $f(x)$ represents the forcing term or input.

- The Green's function $G(x,z)$ must satisfy the exact same homogeneous linear differential equation as $y(x)$, but with the forcing term replaced by the Dirac delta function $\delta(x-z)$: $$L G(x, z) = \delta(x-z)$$

- Physically, $G(x,z)$ may be thought of as the response of the system at position $x$ resulting from a unit-strength point source located at position $z$.


## Green's functions: Introduction (continued)


- Once the Green's function is known, the solution $y(x)$ to the original inhomogeneous system $Ly(x)=f(x)$ is found by integrating the forcing term $f$ against the Green's function $G(x,z)$: $$y(x) = \int_{a}^{b} G(x, z) f(z) dz$$

- This integral means that the response of the system to a distributed force $f(x)$ is the superposition of the individual responses ($G$) to infinitesimal impulses comprising $f(x)$.

- In linear systems, the solution to forced dynamics is given by the **convolution** of the input (forcing) with the system's impulse response. The integral form above shows that the Green's function serves as the **impulse response function** $g(t)$ generalized to differential operators.

- In case of the linear differential operator $L$, the solution is formally $y=L^{-1}f$. The Green's function $G(x,z)$ is the kernel that defines this inverse linear operator $L^{-1}$ through integration.


## Driving force


- Let's consider an inhomogeneous linear equation: $$D y(t) = f(t)$$ $D$ is a linear operator that represents any linear ordinary differential equation (ODE).

- $f(t)$ represents some driving force acting on the system. We can view it as a sum of delta functions: $$f(t) = \sum_i c_i \delta(t - t_i)$$

- In continuous form, this becomes $$f(t) = \int_0^\infty f(t') \delta(t' - t) \, dt'$$

- This representation implies that $f(t)$ can be thought of as a sum of \"kicks\" applied to the system at different times. If we find the response of the system to one of these kicks, then we can just sum up all of these responses for each kick to get the total response. This is the idea of the Green's function.


## Green's function definition


- Next, we define the Green's function as the solution to the ODE with a delta function as the source term.

- The Green's function $G(t, t')$ satisfies: $$D y(t) = f(t)$$ $$D G(t, t')  = \delta(t - t')$$ $$y(t) = \int_0^t G(t, t') f(t') \, dt'$$

- The Green's function $G(t, t')$ represents the system's response to a delta function \"kick\" applied at time $t'$.

- The solution $y(t)$ to the original differential equation can be written as a convolution of the source term $f(t')$ with the Green's function $G(t, t')$.


## Proof for the Green's function


- We can verify the equation $y(t) = \int_0^t G(t, t') f(t') \, dt'$ is a solution by substituting it back into the original ODE $D y(t) = f(t)$:

  $$D y(t) = D \left( \int_0^t G(t, t') f(t') \, dt' \right)$$

- Since $D$ is a linear operator that operates on $t$, it can be applied inside the integral: $$D y(t) = \int_0^t (D G(t, t')) f(t') \, dt'$$

- By definition, the Green's function $G(t, t')$ satisfies the following equation: $$D G(t, t') = \delta(t - t')$$

- Substituting this back into the expression for $D y(t)$: $$D y(t) = \int_0^t \delta(t - t') f(t') \, dt' = f(t)$$


## Proof for the Green's function (continued)

$$D y(t) = \int_0^t \delta(t - t') f(t') \, dt' = f(t)$$

- This confirms that $y(t)$ satisfies the original inhomogeneous equation, proving that the convolution of the Green's function with the source term $f(t')$ is indeed the solution for any linear differential operator $D$.


## Example: Driven damped harmonic oscillator


- Consider the driven damped harmonic oscillator: $$y''(t) + 2\gamma y'(t) + \omega_0^2 y(t) = f(t)$$ where $\gamma$ is the damping coefficient and $\omega_0$ is the natural frequency of the undamped system.

- We start by defining the Green's function $G(t, t')$ for this system, which satisfies: $$G''(t, t') + 2\gamma G'(t, t') + \omega_0^2 G(t, t') = \delta(t - t')$$ with initial conditions $G(0, t') = 0$ and $G'(0, t') = 0$.

- To find $G(t, t')$, we take the Laplace transform of both sides. Let $\mathcal{L}\{G(t, t')\} = \tilde{G}(p, t')$, where $p$ is the Laplace variable.

- The Laplace transform of the differential equation yields: $$p^2 \tilde{G}(p, t') - p G(0, t') - G'(0, t') + \\ 2\gamma \left( p \tilde{G}(p, t') - G(0, t') \right) + \omega_0^2 \tilde{G}(p, t') = \mathcal{L}\{\delta(t - t')\}$$


## Example: Driven damped harmonic oscillator (continued)


- The Laplace transform of the differential equation yields: $$p^2 \tilde{G}(p, t') - p G(0, t') - G'(0, t') + \\ 2\gamma \left( p \tilde{G}(p, t') - G(0, t') \right) + \omega_0^2 \tilde{G}(p, t') = \mathcal{L}\{\delta(t - t')\}$$

- Given the initial conditions $G(0, t') = 0$ and $G'(0, t') = 0$, this simplifies to: $$(p^2 + 2\gamma p + \omega_0^2) \tilde{G}(p, t') = e^{-p t'}$$

- Thus: $$\tilde{G}(p, t') = \frac{e^{-p t'}}{p^2 + 2\gamma p + \omega_0^2}$$

- We now take the inverse Laplace transform to find $G(t, t')$: $$G(t, t') = \frac{1}{\omega_d} e^{-\gamma (t - t')} \sin[\omega_d (t - t')], \quad t' < t$$ where $\omega_d = \sqrt{\omega_0^2 - \gamma^2}$ is the damped frequency.

- This Green's function represents the system's response to an impulsive force applied at time $t'$.


## Example: Driven damped harmonic oscillator (continued)


- Using the Green's function, the general solution to the original equation is given by: $$y(t) = \int_0^t G(t, t') f(t') \, dt'$$

- Substituting the expression for $G(t, t')$: $$y(t) = \frac{1}{\omega_d} \int_0^t e^{-\gamma (t - t')} \sin[\omega_d (t - t')] f(t') \, dt'$$

- This solution shows that the system's response at time $t$ is a sum of the responses to the source term $f(t')$ at all previous times $t'$, weighted by the Green's function. The Green's function incorporates both the damping and the oscillatory nature of the harmonic oscillator.


## System of linear equations


- Let's try to solve matrix equation $$(E\mathbf{I} - \mathbf{H})\mathbf{a} = \mathbf{b},$$ where $E$ is constant, $\mathbf{H}$ is NxN Hermitian matrix, $\mathbf{a}$, $\mathbf{b}$ are vectors.

- The standard approach is to find inverse of $(E\mathbf{I} - \mathbf{H})$: $$\mathbf{G}(E) = (E\mathbf{I} - \mathbf{H})^{-1}$$

- If $\mathbf{G}(E)$ is found, the original equation can be solved for any $\mathbf{b}$: $$\mathbf{a} = (E\mathbf{I} - \mathbf{H})^{-1}\mathbf{b} = \mathbf{G}(E)\mathbf{b}$$


## System of linear equations (continued)


- Recall that the inverse can be found via eigenvalues and eigenvectors of the original matrix, e.g., $$\mathbf{A}\mathbf{q}_i = \lambda_i \mathbf{q}_i \; \rightarrow \; \mathbf{A}^{-1} = \mathbf{Q}\Lambda^{-1} \mathbf{Q}^{-1},$$ where $\mathbf{Q}$ is matrix of eigenvectors and $\Lambda$ is diagonal matrix of eigenvalues.

- Then if $\mathbf{H}\mathbf{C}^{\alpha} = E_{\alpha}\mathbf{C}^{\alpha},$ $\alpha=1,2,\dots,N$: $$(\mathbf{G}(E))_{ij} = \sum _{\alpha} \frac{C_i^{\alpha}C_j^{\alpha*}}{E-E_{\alpha}}$$

- Elements of $\mathbf{G}(E)$ \"blow up\" (have poles) when E is equal to eigenvalues of $\mathbf{H}$.

- $\mathbf{G}(E)$ is a Green's function associated with $\mathbf{H}$.


## Inhomogeneous differential equation


- Now consider inhomogeneous differential equation: $$(E-\hat{H})a(x) = b(x),$$ where E - parameter, $\hat{H}$ - Hermitian differential operator.\
  We solve for $a(x)$.

- Assume we know eigenvalues and eigenvectors of $\hat{H}$: $$\hat{H} \psi_\alpha (x) = E_{\alpha} \psi_{\alpha}(x)$$

- Expand $a(x)$ and $b(x)$ into eigenstates of $\hat{H}$: $$a(x) = \sum_{\alpha} a_{\alpha} \psi_{\alpha}(x)$$ $$b(x) = \sum_{\alpha} b_{\alpha} \psi_{\alpha}(x)$$ Here, we can compute $b_\alpha= \int dx' \psi_\alpha ^*(x') b(x')$.\
  However, we do not know $a_\alpha$ - these need to be determined.


## Inhomogeneous differential equation (continued)


- Plug in $a(x)$ and $b(x)$ into the original equation: $$\sum_\alpha a_{\alpha}(E-\hat{H})\psi_{\alpha}(x) = \sum_{\alpha} b_{\alpha} \psi_{\alpha}(x)$$

- Since $\psi_\alpha$ are eigenstates of $\hat{H}$: $$\sum_\alpha a_{\alpha}(E-E_\alpha)\psi_{\alpha}(x) = \sum_{\alpha} b_{\alpha} \psi_{\alpha}(x)$$

- Multiplying from left by $\psi_\alpha^*(x)$, integrating and using orthogonality of eigenstates $\psi_\alpha$: $$a_{\alpha}(E-E_\alpha) = b_{\alpha}$$

- Solution: $$a(x) = \sum_{\alpha} \frac{ b_{\alpha}}{E-E_\alpha}\psi_\alpha(x)=
          \int dx' \Bigl[ \sum_\alpha \frac{\psi_\alpha(x)\psi_\alpha^*(x')}{E-E_\alpha}  \Bigr] b(x')$$


## Inhomogeneous differential equation (continued)

$$a(x) = \sum_{\alpha} \frac{ b_{\alpha}}{E-E_\alpha}\psi_\alpha(x)=
        \int dx' \Bigl[ \sum_\alpha \frac{\psi_\alpha(x)\psi_\alpha^*(x')}{E-E_\alpha}  \Bigr] b(x')$$

- Green's function $G(x,x',E)$: $$G(x,x',E) = \sum_\alpha \frac{\psi_\alpha(x)\psi_\alpha^*(x')}{E-E_\alpha}$$

- Then solution can be written as: $$a(x) = \int dx' G(x,x',E) b(x')$$

- Similarly to matrix equation, solution to the differential equation with any input (driving force) is known as long as its Green's function is found.

- Similarly to matrix equation, $G$ has poles at $E$ equal to the eigenvalues of $\hat{H}$.


## Inhomogeneous differential equation (continued)


- Suppose $b(x) = \delta(x-x')$

- Then: $$a(x) = \int dx" G(x,x",E) \delta(x'-x") =  G(x,x',E)$$

- Substituting in the original differential equation: $$(E-\hat{H})G(x,x',E) = \delta(x-x')$$

- This is differential equation for $G(x,x',E)$.

- In matrix form: $$(E\mathbf{I} - \mathbf{H})(E\mathbf{I} - \mathbf{H})^{-1} = \mathbf{1}$$

---

*Migration source: `06_ODE/Green.tex` from the archived Overleaf export.*
