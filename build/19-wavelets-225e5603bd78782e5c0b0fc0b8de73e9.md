# Wavelets and Time-Frequency Analysis

:::{admonition} Course-material provenance
:class: dropdown
This page was migrated from the CHM 696 course materials developed by Jonathan Hood and Lyudmila Slipchenko. Equations and examples remain under editorial review during the Fall 2026 website migration.
:::

## Gabor transform


- **Problem:** Although the Fourier transform provides detailed information about the frequency content of a given signal, it does not give any information about when in time those frequencies occur.

- For a signal with non-stationary frequency content, such as a musical composition, it is important to simultaneously characterize the frequency content and its evolution in time.

- The [Gabor transform]{style="color: blue"}, aka the short-time Fourier transform (STFT), computes a windowed FFT in a moving window.

- This STFT enables the localization of frequency content in time, resulting in the [spectrogram]{style="color: blue"}, which is a plot of frequency versus time.


:::{note} Original-slide figure pending review
The original lecture refers to `Gabor1.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

## Gabor transform (continued)


:::{note} Original-slide figure pending review
The original lecture refers to `Gabor2.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

The Gabor transform $G$ is given by: 
```{math}
G(f)(t,\omega)=\hat{f}_g(t,\omega)=\sum_{-\infty}^{\infty} f(τ\tau)e^{-i\omega \tau}g^*(\tau -t)d\tau=⟨g_{t,\omega},f⟩,
```
 where $g_{t,\omega}(\tau)$ is: 
```{math}
g_{t,\omega}(\tau) = e^{i\omega \tau}g(\tau - t)
```
 The function $g(t)$ is the kernel, and is often chosen to be a Gaussian: 
```{math}
g(t) = e^{-(t-\tau)^2/a^2}
```


The parameter $a$ determines the spread of the short-time window for the Fourier transform, and $\tau$ determines the center of the moving window.


## Gabor transform (continued)


Spectrogram of Beethoven's \"Sonata Pathétique\" <https://www.youtube.com/watch?v=XJ6XXjAOvmY>


:::{note} Original-slide figure pending review
The original lecture refers to `Gabor4.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

## Uncertainty principles

The time-frequency uncertainty principle: 
```{math}
\biggl( \sum_{-\infty}^{\infty} x^2 |f(x)|^2 dx \biggr)\biggl( \sum_{-\infty}^{\infty} \omega^2 |\hat{f}(\omega)|^2 d\omega \biggr) \ge \frac{1}{16 \pi^2}
```


The function $x^2 |f(x)|^2$ is the dispersion about $x = 0$. For real-valued functions, this is the second moment, which measures the variance if $f(x)$ is a Gaussian function.

In other words, a function $f(x)$ and its Fourier transform cannot both be arbitrarily localized.


## Wavelets


Wavelets exploite a multi-resolution decomposition that enables different time and frequency fidelities in different frequency bands.


:::{note} Original-slide figure pending review
The original lecture refers to `wavelet1.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

## Wavelets (continued)

Basis functions in wavelet decomposition are as follows:

- Start with mother wavelet $\psi(t)$

- Build a family of functions 
```{math}
\psi_{a,b} (t) = \frac{1}{\sqrt{a}} \psi \bigl( \frac{t-b}{a} \bigr)
```


- $a$ scales $\psi$, i.e, going to higher frequencies

- $b$ translates $\psi$, i.e., moving left and right along a particular frequency

Continuous [wavelet transform]{style="color: blue"}: 
```{math}
W_{\psi}(f)(a,b) = \langle \psi_{a,b}, f \rangle = \sum_{-\infty}^{\infty} \psi_{a,b}^*(t) f(t)
```


## Haar wavelet

The simplest and earliest example of a wavelet is the Haar wavelet, developed in 1910: 
```{math}
\psi(t) =
\begin{cases}
  1, & 0 \le t < \tfrac{1}{2}, \\[6pt]
 -1, & \tfrac{1}{2} \le t < 1, \\[6pt]
  0, & \text{otherwise.}
\end{cases}
```


:::{note} Original-slide figure pending review
The original lecture refers to `wavelet2.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

## Variety of wavelets

There are very many forms of wavelet mother functions used around. Here are some examples:


:::{note} Original-slide figure pending review
The original lecture refers to `wavelet3.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

## 2D Fourier transform for images


:::{note} Original-slide figure pending review
The original lecture refers to `FFT1.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

The two-dimensional Fourier transform of a matrix of data $\mathbf{X} \in \mathbb{R}^{n×m}$ is achieved by first applying the one-dimensional Fourier transform to every row of the matrix, and then applying the one-dimensional Fourier transform to every column of the intermediate matrix.

Applications:

- Image compression

- Image denoising


## FFT2 image compression


:::{note} Original-slide figure pending review
The original lecture refers to `FFT2.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

## 2D wavelet image compression

Similar to the FFT, the discrete wavelet transform is extensively used for image processing and compression.

In the figure below, the hierarchical nature of the wavelet decomposition is seen.

The upper left corner of the DWT image is a low-resolution version of the image, and the subsequent features add fine details to the image.


:::{note} Original-slide figure pending review
The original lecture refers to `wavelet4.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

## 2D wavelet image compression (continued)

The hierarchical representation of data in the wavelet transform is ideal for image compression.

Even with an aggressive truncation, retaining only 0.5$\%$ of the DWT coefficients, the coarse features of the image are retained.

Thus, when transmitting data, even if bandwidth is limited and much of the DWT information is truncated, the most important features of the data are transferred.


:::{note} Original-slide figure pending review
The original lecture refers to `wavelet5.png`. The file is preserved in the private Overleaf archive and will be redrawn or published after its reuse rights are verified.
:::

---

*Migration source: `05_Fourier/Fourier3.tex` from the archived Overleaf export.*
